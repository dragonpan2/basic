{
  "name": "base",
  "version": "1.0.0",
  "description": "Project Base, with Firebase and NodeJS deploy on heroku",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "firebase": "^5.10.0"
  }
}
